{
  "name": "tiy-web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "parcel build src/*.html --out-dir dist ./"
  },
  "devDependencies": {
    "node-sass": "^7.0.0",
    "parcel-bundler": "^1.12.5"
  }
}
